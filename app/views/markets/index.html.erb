<h1>All Markets</h1>

<p><%= link_to("Create a new market", new_market_path, method: :get) %></p>

<h1><% @markets.each do |market| %>
  <%= "Market: #{market.name}" %>
  <%= link_to("View details", market_path(market), method: :get) %>
<% end %></h1>

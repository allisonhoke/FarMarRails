<nav class = "nav">
<ul class="navigation">
<li class="nav"><a href="/">Home</a></li>
<li class="nav"><a href="/"></a></li>
<li class="nav"><%= link_to("Create a new product", new_vendor_product_path(@vendor), method: :get) %></li>

<h1>Your Current Products</h1>
<% @individual_products.each do |t| %>
  <h2><%= "Product: #{t.name}" %></h2>
  <%= link_to("Edit this product", edit_vendor_product_path(@vendor, t), method: :get) %></p>
  <%= link_to("Delete this product", vendor_product_path(@vendor, t), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }) %></p>
  <%= link_to("Create a sale for this product", new_vendor_sale_path(@vendor, {product_id: t.id}) ) %>
 <% end %>
</ul>
</nav>
